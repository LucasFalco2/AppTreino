const router=require("express").Router();const multer=require("multer");const path=require("path");const fs=require("fs");const {randomUUID}=require("crypto");const {requireAuth}=require("../middleware/auth");const {requireRole,requireOwnStudentOrAdmin}=require("../middleware/role");const ctrl=require("../controllers/workouts.controller");
router.use(requireAuth);router.get("/exercises",ctrl.listExercises);router.get("/students/:studentId/workout",requireOwnStudentOrAdmin,ctrl.getActiveWorkout);router.get("/students/:studentId/workout-logs",requireOwnStudentOrAdmin,ctrl.getLoadHistory);router.post("/workout-logs",requireOwnStudentOrAdmin,ctrl.registerLoad);router.post("/exercise-completions",requireOwnStudentOrAdmin,ctrl.completeExercise);router.post("/",requireRole("ADMIN"),ctrl.createWorkout);router.put("/:id",requireRole("ADMIN"),ctrl.updateWorkout);
const dir=path.join(process.cwd(),"uploads","videos");fs.mkdirSync(dir,{recursive:true});const upload=multer({storage:multer.diskStorage({destination:dir,filename:(req,file,cb)=>cb(null,randomUUID()+path.extname(file.originalname))}),limits:{fileSize:100*1024*1024},fileFilter:(req,file,cb)=>cb(null,file.mimetype.startsWith("video/"))});
router.post("/exercises",requireRole("ADMIN"),ctrl.createExercise);router.patch("/exercises/:id",requireRole("ADMIN"),ctrl.updateExercise);router.delete("/exercises/:id",requireRole("ADMIN"),ctrl.deleteExercise);router.post("/exercises/:exerciseId/video",requireRole("ADMIN"),upload.single("video"),ctrl.addExerciseVideo);router.put("/exercises/:exerciseId/video/:videoId",requireRole("ADMIN"),upload.single("video"),ctrl.replaceExerciseVideo);router.delete("/exercises/:exerciseId/video/:videoId",requireRole("ADMIN"),ctrl.deleteExerciseVideo);
module.exports=router;
